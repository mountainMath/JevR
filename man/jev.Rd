% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jev.R
\name{jev}
\alias{jev}
\title{Ask Jev typed questions about a state}
\usage{
jev(
  state,
  questions,
  model = "jev-latest",
  api_key = jev_api_key(),
  max_tries = 5,
  timeout = 60
)
}
\arguments{
\item{state}{The content to evaluate: a string, or a named list (or one-row
data frame) of named fields for structured context. Questions can refer to
fields with backticked paths such as \code{`review.text`}.}

\item{questions}{A named list of questions built with \code{\link[=jev_noul]{jev_noul()}},
\code{\link[=jev_choice]{jev_choice()}} or \code{\link[=jev_score]{jev_score()}}. The names are the ids under which answers
are returned; they are not sent to the model.}

\item{model}{Model name or alias. \code{"jev-latest"} is the current stable
release; see \code{\link[=jev_models]{jev_models()}}.}

\item{api_key}{TypeSafe API key. Defaults to the \code{TYPESAFE_API_KEY}
environment variable.}

\item{max_tries}{Number of attempts on rate-limit (429) or overloaded (529)
responses, with exponential backoff.}

\item{timeout}{Request timeout in seconds.}
}
\value{
An object of class \code{jev_response}: a list with \code{model} (the
versioned model that answered), \code{answers} (one per question id) and
\code{usage} (input and output tokens). Use \code{\link[=jev_tidy]{jev_tidy()}}, \code{\link[=jev_probabilities]{jev_probabilities()}}
or \code{\link[=jev_answer]{jev_answer()}} to work with the answers.
}
\description{
Sends one request to TypeSafe's System One endpoint. All questions are
evaluated against the same \code{state} in parallel and cannot see each other's
answers, so put independent questions in one call.
}
\examples{
\dontrun{
res <- jev(
  "Help! My payouts have been failing for 3 days.",
  list(
    urgent = jev_noul("Does this convey urgency?"),
    mood = jev_score("How does the writer feel?",
                     c("Calm", "Frustrated", "Very angry"))
  )
)
jev_tidy(res)
jev_probabilities(res, "mood")
}
}
\seealso{
\code{\link[=jev_map]{jev_map()}} to run the same questions over many states.
}
