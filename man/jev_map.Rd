% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{jev_map}
\alias{jev_map}
\title{Run the same questions over many states}
\usage{
jev_map(
  states,
  questions,
  model = "jev-latest",
  api_key = jev_api_key(),
  probabilities = FALSE,
  pause = 0,
  progress = TRUE,
  on_error = c("warn", "stop"),
  keep_response = FALSE,
  max_tries = 5,
  timeout = 60
)
}
\arguments{
\item{states}{The records to evaluate: a character vector (one string per
state), a list of states (strings or named lists), or a data frame whose
rows become named-list states (columns with \code{NA}, \code{NULL} or empty values
are dropped per row; list columns are unwrapped).}

\item{questions}{A named list of questions built with \code{\link[=jev_noul]{jev_noul()}},
\code{\link[=jev_choice]{jev_choice()}} or \code{\link[=jev_score]{jev_score()}}. The names are the ids under which answers
are returned; they are not sent to the model.}

\item{model}{Model name or alias. \code{"jev-latest"} is the current stable
release; see \code{\link[=jev_models]{jev_models()}}.}

\item{api_key}{TypeSafe API key. Defaults to the \code{TYPESAFE_API_KEY}
environment variable.}

\item{probabilities}{Passed to \code{\link[=jev_tidy]{jev_tidy()}}.}

\item{pause}{Seconds to wait between requests.}

\item{progress}{Show a progress bar (see \code{\link[purrr:map]{purrr::map()}}).}

\item{on_error}{What to do when a request fails after retries: \code{"warn"}
(default) keeps going and leaves that row's answers \code{NA}, recording the
message in \code{.error}; \code{"stop"} aborts.}

\item{keep_response}{Also keep every raw \code{jev_response} in a \code{.response}
list column.}

\item{max_tries}{Number of attempts on rate-limit (429) or overloaded (529)
responses, with exponential backoff.}

\item{timeout}{Request timeout in seconds.}
}
\value{
A tibble with one row per state: the \code{\link[=jev_tidy]{jev_tidy()}} columns, plus
\code{.error} (\code{NA} when the call succeeded) and optionally \code{.response}.
Bind it back onto your data with \code{\link[dplyr:bind_cols]{dplyr::bind_cols()}} or keep an id column
in \code{states}.
}
\description{
Calls \code{\link[=jev]{jev()}} once per state and stacks the tidied answers into a tibble
with one row per state, in input order. Failed calls are recorded rather
than aborting the whole run.
}
\examples{
\dontrun{
qs <- list(
  cost = jev_noul("Does the reviewer complain about cost?"),
  sentiment = jev_score("How does the reviewer feel overall?",
                        c("Very negative", "Negative", "Neutral", "Positive", "Very positive"))
)
reviews <- data.frame(comment = c("Great dentist, no pain at all.",
                                  "Charged me double what was quoted."))
jev_map(reviews, qs)
}
}
